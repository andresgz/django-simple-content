============
Installation
============

At the command line::

    $ easy_install django-simple-content

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv django-simple-content
    $ pip install django-simple-content